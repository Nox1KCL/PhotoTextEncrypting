# PhotoTextEncrypting (Steganography Tool)

Інструмент для стеганографії, реалізований на Python. Дозволяє приховувати текстову інформацію всередині графічних файлів (PNG) шляхом маніпуляції найменш значущими бітами (LSB - Least Significant Bit) пікселів.

Проєкт демонструє роботу з неструктурованими даними (зображеннями) на низькому рівні.

### 🛠 Технічні деталі (Low-level Data Processing):
* **Bitwise Operations:** Використання бітових масок (`& 254`) та операторів (`|`, `%`) для точкової зміни RGB-каналів без візуальної втрати якості зображення.
* **Data Encoding Pipelines:** Перетворення текстових даних у бінарний потік (`text_to_bits`) та зворотна реконструкція.
* **Memory Efficiency:** Використання **Python Generators** (`yield` у функції `current_bit`) для потокової подачі бітів, що дозволяє обробляти великі обсяги даних без перевантаження пам'яті.

### 📋 Алгоритм роботи:
1. **Encoding:** Зображення розкладається на піксельну матрицю. Текст конвертується в біти. Кожен біт тексту "вшивається" в LSB каналів R, G, B.
2. **Stop Marker:** Використання спеціального маркера (`#####`) для позначення кінця повідомлення, що дозволяє коректно зупинити декодування.
3. **Decoding:** Сканування пікселів, екстракція LSB та збірка байтів назад у символи до моменту знаходження маркера.

### 🚀 Stack:
* **Language:** Python 3.13
* **Libraries:** `Pillow` (PIL) для маніпуляції матрицями пікселів.
* **Concepts:** Binary Arithmetic, Image Processing, Generators.

### Usage:
Скрипт зчитує зображення, кодує в нього заданий текст і зберігає результат. Декодер автоматично знаходить прихований контент.
